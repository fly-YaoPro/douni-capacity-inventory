---
name: douni-capacity-inventory
description: 豆尼·银手的每日产能盘点。用于“产能盘点”“每日产能”“本周确定产能”“机构产能”“专项产能”等请求，仅按本周范围统计抖音、小红书发布、审核与临近发布阶段；全量在执行数量不进入本报告。
---

# 豆尼产能盘点

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douni-capacity-inventory
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 目标

按字段权威来源读取客户表与机构多维表，回答“本周较确定能发布多少”。确定性代码负责统计，模型不得猜数。

## 执行

1. 完整读取 [business-rules.md](references/business-rules.md)。
2. 在 `E:\豆尼·银手` 调用 `CapacityInventoryService`；人工校验可运行 `npm run capacity:once`。
3. 客户侧发布与审核读取 6 张客户 Sheet：原抖音/小红书发布与审核 4 张，加独立端发布与审核 2 张；机构归属、唯一 ID和本周待执行/拍摄中读取 Base 中全部“机构 X 焦尾 执行表”，当前最低基线为 13 张，实际数量动态发现。
4. 任一客户 Sheet 缺失、机构表少于基线、字段缺失、分页截断或读取期间 revision 不稳定时失败关闭。
5. 输出顺序：分平台确定产能、抖音目标差额、本周待执行/拍摄中、机构统计、项目/专项分类、待核机构归属、数据完整性。
6. 盘点前的发布表更新由 Daemon 编排；少量待补不得让整份盘点静默失败，按客户表已经登记的事实统计并显著标注缺口。

## 来源原则

- 客户发布表是正式发布事实的权威来源；客户审核表只提供客户审核状态，并且必须按本周周期过滤。
- 机构多维表是唯一 ID、机构归属和当前执行进度的实时主账；不再读取旧机构“专项提报/专项执行”Sheet。
- 全量表用于工作台和跨机构分析，本 Skill 不依赖其同步是否刚完成。
- 旧机构电子表格仅供历史修复，不进入每日产能链路。

## 发送与调度

- 正式调度结果只发“焦尾-豆包用增”；客户群、供应商群不接收。
- 本 Skill 不定义执行时间；Daemon 管理时间和“盘点前先更新发布表”的编排。
- 23:00 任务若跨午夜补跑，仍使用前一业务日，避免“今日发布”归零。

## 质量要求

- 抖音周目标固定为 50；小红书当前不设目标，两端不得混算差额。
- 发布/审核记录匹配机构时依次使用：唯一 ID、稿件链接、主页链接、平台与执行日期；客户发布表有物理行机构色块时，色块仍是该发布行的直接证据。仅凭昵称无法唯一确认时必须留待核。
- 客户审核表仍停留在待发布等状态、但同一稿件已经发布时，按发布事实去重。
- 本周发布以周期为主、发布时间兜底，保证今日发布属于本周发布。
- 客户审核表中的待发布、待客户审核、修改后发布必须属于本周周期；历史周期和无有效周期记录不得计入本周确定产能。
- 发布、审核及机构待执行记录使用公共客户专项分类：教程（3W以下课程教学）、系列教程（原课程教学）、行业用例（原工作任务）、玩法追热分别统计，不能跳过或合并。独立端及双端兼容现有来源；合作包不自动生成两条产能，按实际两期记录计算。Excel、春节专项等其他项目不得混入。
- 确定产能 = 本周发布 + 待发布 + 待客户审核 + 修改后发布，不含待执行/拍摄中。
- 本报告不输出全量在执行数量；若另行判断执行状态，客户已通过但进度为空应解释为“待执行”，不是异常状态。待发布仍属于在执行。
- 待执行/拍摄中只统计预计发布日期或预定档期落在本周的记录；客户通过且进度为空按待执行处理。
- 客户记录按自身项目字段归类，机构匹配只补机构归属，不能用机构旧专项覆盖客户细分类。别名来自公共业务口径，不在 Skill 内另存一套判定逻辑。
- 遥师确认待客户审核继续计入确定产能；群聊追问必须区分当日发布、本周发布、本周确定产能，不得用单张发布表的当日数字替换确定产能。核对卡片时先读取对应业务日的产能运行记录。
